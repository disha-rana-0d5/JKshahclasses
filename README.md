
  # Sticky Global Navigation Bar (Copy)

  This is a code bundle for Sticky Global Navigation Bar (Copy). The original project is available at https://www.figma.com/design/dwSLBnxLHkk8FTDDwzq24W/Sticky-Global-Navigation-Bar--Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  